For low latency and strict governance, local deployment is generally better than cloud.
Cloud is easier operationally and can provide stronger large-model quality, but has higher governance risk and network latency variability.

Use PostgreSQL + pgvector with Python services (FastAPI, SQLAlchemy, Alembic).
Consider hybrid fallback for non-sensitive tasks.
